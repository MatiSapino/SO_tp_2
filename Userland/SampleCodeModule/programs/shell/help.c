#include <help.h>
#include <buffer.h>

int help(int argc, char *argv[])
{
    own_printf("Command list:\n", GREEN);
    // for (int i = 0; i < COMMAND_AMOUNT; i++)
    // {
    //     own_printf(" - %s\n", command_list[i]);
    // }

    return 0;
}